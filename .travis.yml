language: cpp
sudo: required
cache:
  apt: true
env:
  global:
  - CXX=g++-4.8

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8

script: mkdir build && cd build && cmake .. && make
